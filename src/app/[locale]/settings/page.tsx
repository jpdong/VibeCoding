import SettingsPageComponent from './PageComponent';

export default function SettingsPage() {
  return <SettingsPageComponent />;
}

export const metadata = {
  title: 'Settings - VibeCoding',
  description: 'Manage your VibeCoding account settings, subscription, and preferences',
};